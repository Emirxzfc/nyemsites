## Packages
framer-motion | Complex animations for the "wow" factor
lucide-react | Icons for stats and UI elements
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Tailwind Config - extend colors:
colors: {
  neon: {
    DEFAULT: "#39FF14", // Bright neon green
    glow: "#4ADE80",    // Softer glow green
    dark: "#14532D",    // Dark green background accent
  },
  dark: {
    DEFAULT: "#0a0a0a", // Deep black
    card: "#171717",    // Slightly lighter black for cards
  }
}
fontFamily: {
  display: ["'Rajdhani'", "sans-serif"],
  body: ["'Inter'", "sans-serif"],
}
